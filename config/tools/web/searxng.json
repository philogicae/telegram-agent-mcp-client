{
  "command": "SEARXNG_URL={ENV:SEARXNG_URL} npx -y searxng-mul-mcp",
  "edit": {
    "search": {
      "name": "web_search",
      "description": "Perform web search(es) across multiple search engines, support searching multiple queries in parallel, and return detailed results including titles, URLs, and summaries."
    }
  }
}
/* all found tools: 1
search */
